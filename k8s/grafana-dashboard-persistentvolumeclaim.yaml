apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: grafana-dashboard
  name: grafana-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
