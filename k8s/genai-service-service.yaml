apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    io.kompose.service: genai-service  # <- Optional, but useful for grouping
spec:
  selector:
    io.kompose.service: genai-service  # <- Required for routing
  ports:
    - port: 8000
      targetPort: 8000
