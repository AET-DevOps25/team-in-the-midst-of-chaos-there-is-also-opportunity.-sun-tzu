apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: grafana-provisioning
  name: grafana-provisioning
  namespace: aifm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
