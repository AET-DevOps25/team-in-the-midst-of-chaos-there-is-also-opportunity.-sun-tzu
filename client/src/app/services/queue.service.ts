import { Injectable, Signal, signal } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class QueueService {

  
  // queue: Song[] = [
  // ]

  // nextAudios: Signal<Track[]> = signal([])

}
